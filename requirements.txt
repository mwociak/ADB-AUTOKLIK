# Freebuff Desktop - zależności (Python 3.11+, Windows)
#
# Instalacja:
#   pip install -r requirements.txt
#
# Uwaga: adbutils wymaga binarnego `adb` (platform-tools) w PATH.

# GUI
PyQt6>=6.6

# Globalne przechwytywanie klawiszy (Keymapper - mapowanie klawiszy laptopa)
pynput>=1.7.6

# Czysto-pythonowy klient ADB (Keymapper via ADB, połączenie ze scrcpy)
adbutils>=2.6

# Klient scrcpy do podglądu ekranu Androida (stream H.264 przez ADB)
# Uwaga: pakiet "scrcpy-py" nie istnieje na PyPI, a "scrcpy-client"
# opublikowany na PyPI (0.4.7, luty 2024) ma PRZESTARZAŁE zależności:
# wymaga adbutils>=1.0.8,<2.0.0, av>=9,<10 i ciągnie opencv-python -
# koliduje z resztą projektu (ERROR: ResolutionImpossible).
# Dlatego instalujemy z git main (leng-yue/py-scrcpy-client), którego
# metadane wymagają adbutils>=2,<3, av>=12,<13 i numpy>=2,<3 - w pełni
# zgodnych z resztą tego pliku. Przypięte do commita ad57b15 dla
# reprodukowalnych instalacji.
scrcpy-client @ git+https://github.com/leng-yue/py-scrcpy-client.git@ad57b15934c71cb24555bbb3f5050b1d6c908de5

# Dekoder wideo (PyAV) i numpy - zależności scrcpy-client do ramek obrazu
av>=12
numpy>=2
